fn apply_query(entity, operation, user, payload) {
    if entity == "command" {
        throw "Opertaion not permitted."
    }
    return
}

fn check_rule(entity, operation, user, resource) {
    return false;
}

